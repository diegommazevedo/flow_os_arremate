import { WorkflowEditor } from "./_components/WorkflowEditor";

export const metadata = { title: "Workflow Editor — Motoboys — FlowOS" };

export default function WorkflowPage() {
  return <WorkflowEditor />;
}
