AUDITORIA FlowOS — 2026-03-31 — Gate S3-03

CRÍTICO:
- [P-01] VIOLAÇÃO DE NÚCLEO confirmada em `packages/brain/src/agents/relatorio-imovel.ts`: o agente contém vocabulário proibido do domínio (`boleto`, `caixa`, `arrematante`) e ainda referencia termos imobiliários adicionais (`imovel`, `matricula`, `averbacao`) no prompt, HTML e metadados.
- [P-01] VIOLAÇÃO DE NÚCLEO confirmada em `packages/brain/src/agents/__tests__/relatorio-imovel.test.ts`: o teste replica vocabulário proibido do domínio (`ITBI`, `boleto`, `caixa`, `registro`) e termos imobiliários adicionais (`imovel`, `matricula`, `averbacao`).
- [P-01] Violações de núcleo continuam presentes no estado atual de `packages/brain/src/index.ts`, `packages/brain/src/agents/boleto-recovery.ts`, `packages/brain/src/token-router.test.ts` e `packages/core/src/domain/types.ts`.

ALERTA:
- [EXCEÇÃO-ADAPTADOR-EXTERNO] `packages/brain/src/workers/rpa-caixa.ts` foi tratado como exceção explícita do gate: `imovelId`, `averbacao` e `matricula` são campos do CSV externo da Caixa persistidos em `Deal.meta:Json`; o arquivo também contém outros tokens do portal (`caixa`, `boleto`, `corretor`, `arrematante`) compatíveis com o papel de adaptador externo e não foram contabilizados como violação P-01 deste gate.
- [MULTI-TENANT] `packages/db/prisma/schema.prisma` não define `orgId` no model `Document`; a chave de tenant é `workspaceId` (`model Document` em `schema.prisma:126`, campos em `:128-129`). A verificação “toda query em Document terá orgId via deal.orgId” falhou no estado atual: `packages/brain/src/agents/relatorio-imovel.ts:359` faz `prisma.document.findUnique({ where: { id: docId } })` e `packages/brain/src/agents/relatorio-imovel.ts:367` faz `prisma.document.update({ where: { id: docId } })` sem `workspaceId` nem `dealId`. O mesmo arquivo também busca `Deal` apenas por id em `packages/brain/src/agents/relatorio-imovel.ts:387`, ignorando `payload.workspaceId`.
- [SEC-08] `packages/brain/src/agents/relatorio-imovel.ts` não usa `defaultSanitizer`; há apenas escaping HTML via `escHtml()` (`relatorio-imovel.ts:315`) para o PDF renderizado. O arquivo continua montando prompt e metadados vetoriais a partir de `dealMeta`, `faqChunks` e `pastChunks` sem sanitização equivalente. Em contrapartida, `packages/brain/src/workers/rpa-caixa.ts` atende SEC-08 com `defaultSanitizer.sanitize()` em `:24` e `:365-450`.
- [NOVOS ARQUIVOS] O workspace auditado não contém `.git`; o conjunto “novos arquivos do gate S3-03” foi inferido por timestamp e pela superfície do recurso (`relatorio-imovel.ts`, `rpa-caixa.ts`, testes associados e `schema.prisma`).

DEBT:
- Contagens globais: `any=0`, `TODO=1`, `FIXME=0`, `console.log=37`, `@ts-ignore=0`.
- `TODO` confirmado em `apps/web/src/app/portal/[dealId]/_lib/portal-queries.ts:401`.
- Funções com mais de 50 linhas no projeto: `35`.
- Funções >50 linhas introduzidas/afetadas no gate S3-03:
- `packages/brain/src/agents/relatorio-imovel.ts:167` — `renderHtml` (146 linhas)
- `packages/brain/src/agents/relatorio-imovel.ts:379` — `generateRelatorioImovel` (124 linhas)
- `packages/brain/src/workers/rpa-caixa.ts:180` — `parseCsv` (53 linhas)
- `packages/brain/src/workers/rpa-caixa.ts:243` — `scrapePortalCsv` (72 linhas)
- `packages/brain/src/workers/rpa-caixa.ts:346` — `processRow` (128 linhas)
- `packages/brain/src/workers/rpa-caixa.ts:477` — `runRpaCaixa` (158 linhas)
- `packages/brain/src/workers/__tests__/rpa-caixa.test.ts:63` — `buildMockPrisma` (67 linhas)
- Arquivos sem teste correspondente no projeto: `55`.
- Cobertura dos novos arquivos centrais do gate: `packages/brain/src/agents/relatorio-imovel.ts` possui `packages/brain/src/agents/__tests__/relatorio-imovel.test.ts`; `packages/brain/src/workers/rpa-caixa.ts` possui `packages/brain/src/workers/__tests__/rpa-caixa.test.ts`.
- SEC-02 nos arquivos novos auditados: nenhum segredo hardcoded encontrado.
- SEC-06 no estado atual: nenhuma ocorrência de `auditLog.update()`, `auditLog.delete()`, `agentAuditLog.update()` ou `agentAuditLog.delete()` foi encontrada; `packages/brain/src/agents/relatorio-imovel.ts:492` usa `auditWriter.log(...)` e `packages/brain/src/workers/rpa-caixa.ts:644` usa `prisma.rpaLog.create(...)`.

INTEGRIDADE: 100% dos arquivos declarados existem (46/46 arquivos referenciados em `gapsreturn/*.md`)

VEREDICTO: REPROVADO
