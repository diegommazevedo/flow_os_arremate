AUDITORIA FlowOS — 2026-03-31 — Gate S4-02

CRÍTICO:
- [SEC-03][SSE] RISCO DE VAZAMENTO CROSS-ORG confirmado em `apps/web/src/app/api/sse/dashboard/route.ts`: o endpoint não valida autenticação nem autorização antes de abrir o stream; aceita `?workspaceId=...` controlado pelo cliente em `route.ts:23-24` e faz push de `getDashboardMetrics(workspaceId)` em `route.ts:51` e `route.ts:67`. Na ausência do parâmetro, cai em `getDefaultWorkspaceId()` e seleciona o primeiro workspace do banco (`apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts:404-409`), o que amplia a superfície de vazamento.
- [P-01] VIOLAÇÃO DE NÚCLEO permanece aberta no projeto: `packages/brain/src/agents/relatorio-imovel.ts`, `packages/brain/src/agents/__tests__/relatorio-imovel.test.ts`, `packages/brain/src/index.ts`, `packages/brain/src/agents/boleto-recovery.ts`, `packages/brain/src/token-router.test.ts` e `packages/core/src/domain/types.ts` continuam com termos de setor proibidos no estado atual.

ALERTA:
- [P-01][Dashboard] O dashboard novo não está limpo de vocabulário setorial: `apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts` expõe o rótulo `RPA Caixa` em múltiplos pontos (`:149`, `:161`, `:174`). A ocorrência está fora de `packages/core/` e `packages/brain/`, mas confirma contaminação de UI por terminologia de setor.
- [SEC-03][Dashboard Queries] As queries operacionais do dashboard estão corretamente filtradas por `workspaceId` em `fetchDeals()` (`dashboard-queries.ts:133-137`), `fetchRpaStats()` (`:142-145`) e `fetchAuditStats()` (`:193-206`). O ponto que quebra o isolamento é o fallback `getDefaultWorkspaceId()` (`:404-409`), usado tanto em `apps/web/src/app/(portal)/dashboard/page.tsx:51-71` quanto no SSE.
- [P-01][migrate-pipedrive] `packages/db/scripts/migrate-pipedrive.ts` não mantém termos de setor confinados apenas a funções de mapeamento. Eles aparecem em tipos, validações, aliases de coluna, mensagens e persistência fora de `detectSubtype()` e `mapSituacaoToStageName()`, por exemplo em `migrate-pipedrive.ts:36-67`, `:256-299`, `:310-312`, `:341-346`, `:407-448` e `:513-592`.
- [migrate-pipedrive] `process.exit(2)` quando há falhas parciais foi tratado como comportamento intencional documentado e não invalida a auditoria.
- [SEC-08][Incremental desde S3-03] O único fluxo novo com entrada externa para LLM continua sendo `packages/brain/src/agents/relatorio-imovel.ts`; ele ainda chama `callClaude`/`callFallback` com dados externos sem `defaultSanitizer` equivalente (`relatorio-imovel.ts:426-431`). Em contraste, `packages/db/scripts/migrate-pipedrive.ts` sanitiza entradas com `sanitize()` em `:124-133` e `:247`, e `packages/brain/src/workers/rpa-caixa.ts` mantém `defaultSanitizer` documentado no cabeçalho e em uso.
- [EXCEÇÃO-ADAPTADOR-EXTERNO] A exceção para `packages/brain/src/workers/rpa-caixa.ts` permanece documentada no cabeçalho do arquivo (`rpa-caixa.ts:1-15`), incluindo a motivação para `imovelId`, `averbacao` e `matricula` como campos externos do CSV da Caixa.

DEBT:
- Contagens globais: `any=0`, `TODO=1`, `FIXME=0`, `console.log=67`, `@ts-ignore=0`.
- `TODO` confirmado em `apps/web/src/app/portal/[dealId]/_lib/portal-queries.ts:401`.
- Funções com mais de 50 linhas no projeto: `44`.
- Funções >50 linhas introduzidas/afetadas no gate S4-02:
- `apps/web/src/app/(portal)/dashboard/_components/AssigneeTable.tsx:41` — `AssigneeTable` (67 linhas)
- `apps/web/src/app/(portal)/dashboard/_components/AssigneeTable.tsx:109` — `AssigneeRow` (63 linhas)
- `apps/web/src/app/(portal)/dashboard/_components/CriticalAlerts.tsx:52` — `AlertRow` (67 linhas)
- `apps/web/src/app/(portal)/dashboard/_components/DashboardClient.tsx:18` — `ToastList` (285 linhas, heurística estrutural)
- `apps/web/src/app/(portal)/dashboard/_components/PipelineChart.tsx:41` — `PipelineChart` (98 linhas)
- `apps/web/src/app/(portal)/dashboard/_components/WorkerStatus.tsx:86` — `WorkerStatus` (53 linhas)
- `apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts:223` — `getDashboardMetrics` (179 linhas)
- `apps/web/src/app/api/sse/dashboard/route.ts:21` — `GET` (76 linhas)
- `packages/db/scripts/migrate-pipedrive.ts:390` — `createDeal` (70 linhas)
- `packages/db/scripts/migrate-pipedrive.ts:463` — `run` (161 linhas)
- Arquivos novos/alterados desde S3-03 sem teste correspondente:
- `apps/web/src/app/(portal)/dashboard/page.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/AssigneeTable.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/CriticalAlerts.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/DashboardClient.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/PipelineChart.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/WorkerStatus.tsx`
- `apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts`
- `apps/web/src/app/api/sse/dashboard/route.ts`
- `packages/db/scripts/migrate-pipedrive.ts`
- SEC-02 na varredura incremental desde S3-03: nenhum segredo hardcoded encontrado.
- SEC-06 na varredura incremental desde S3-03: nenhuma ocorrência de `auditLog.update()`, `auditLog.delete()`, `agentAuditLog.update()` ou `agentAuditLog.delete()` foi encontrada.

INTEGRIDADE: 100% dos arquivos declarados existem (51/51 arquivos referenciados em `gapsreturn/*.md`)

VEREDICTO: REPROVADO
