AUDITORIA FlowOS — 2026-03-31 — Gate S4-02b

CRÍTICO:
- nenhum

ALERTA:
- `apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts`: a função `getDefaultWorkspaceId()` foi removida da lógica, mas a string ainda permanece em comentário em `dashboard-queries.ts:403`. Remoção funcional confirmada; remoção textual completa não foi concluída.
- `packages/db/scripts/migrate-pipedrive.ts`: a restrição “nenhum termo setorial fora de funções de mapeamento marcadas com // [P-01] MAPEAMENTO EXTERNO” ainda não foi plenamente atendida. Há termos setoriais fora do bloco explicitamente marcado, por exemplo em `migrate-pipedrive.ts:50`, `:67`, `:316`, `:347`, `:413`, `:454` e em logs/resultados subsequentes com `imovelId`.
- `apps/web/src/app/api/sse/kanban/route.ts`: não aceita `workspaceId` do request, portanto não viola o critério pedido no item 6, mas continua sendo um SSE sem autenticação de sessão e publica eventos globais do bus.

DEBT:
- Contagens globais: `any=0`, `TODO=1`, `FIXME=0`, `console.log=67`, `@ts-ignore=0`.
- Funções com mais de 50 linhas no projeto: `44`.
- Arquivos sem teste correspondente relevantes nesta reauditoria:
- `apps/web/src/app/(portal)/dashboard/page.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/AssigneeTable.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/CriticalAlerts.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/DashboardClient.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/PipelineChart.tsx`
- `apps/web/src/app/(portal)/dashboard/_components/WorkerStatus.tsx`
- `apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts`
- `apps/web/src/app/api/sse/dashboard/route.ts`
- `packages/db/scripts/migrate-pipedrive.ts`

INTEGRIDADE: 100% dos arquivos declarados existem

VEREDICTO: APROVADO COM RESSALVAS

Notas da reauditoria:
- `apps/web/src/app/api/sse/dashboard/route.ts`: `workspaceId` vem apenas da sessão autenticada em `getSessionWorkspaceId()` (`route.ts:30-53`); retorna `401` se a sessão estiver ausente/inválida (`route.ts:62-64`); e o evento `connected` não inclui `workspaceId` (`route.ts:84`). O payload `metrics` também não expõe `workspaceId` no tipo `DashboardMetrics`.
- `apps/web/src/app/(portal)/dashboard/_lib/dashboard-queries.ts`: nenhum fallback operacional de `workspaceId` restou; todas as queries continuam filtradas por `workspaceId`.
- `apps/web/src/app/(portal)/dashboard/page.tsx`: `redirect("/login")` confirmado quando a sessão é nula (`page.tsx:82-84`).
- `apps/web/src/app/(portal)/dashboard/_components/WorkerStatus.tsx`: não contém termos setoriais; passou na verificação pedida.
- `packages/brain/src/agents/relatorio-imovel.ts`: `sanitizePrompt()` é aplicado aos campos textuais usados no prompt via helper `s()` (`relatorio-imovel.ts:121-124`, `:145-166`) e os chunks do pgvector são sanitizados antes de entrar no prompt (`:142-143`).
- Varredura final do projeto: nenhuma outra `route.ts`/`route.tsx` aceita `workspaceId` vindo do request sem validação de sessão. A busca não encontrou leitura de `workspaceId` via query/body/formData em rotas. O webhook `apps/web/src/app/api/webhooks/rocket/route.ts` não recebe `workspaceId` do request; ele deriva contexto internamente por `roomId`.
- SEC-02: nenhum segredo hardcoded encontrado.
- SEC-06: nenhuma ocorrência de `auditLog.update()`, `auditLog.delete()`, `agentAuditLog.update()` ou `agentAuditLog.delete()` encontrada.
