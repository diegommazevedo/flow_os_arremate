# Opcional: segredos só para MCP (sobrepõem chaves do .env quando repetidas).
# Copie para `.env.mcp` na raiz (ficheiro ignorado pelo git).
#
# MAGIC_21ST_API_KEY=
# GITHUB_PERSONAL_ACCESS_TOKEN=
# MCP_POSTGRES_URL=
# MCP_FILESYSTEM_ROOT=
