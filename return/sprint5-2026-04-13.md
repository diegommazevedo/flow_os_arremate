# Sprint 5 — Seed 15 etapas, paralelas, funil /leads, deploy

**Data:** 2026-04-13

```
DEPLOY RAILWAY: executado git push origin main; novo commit Sprint 5 requer push adicional após este relatório
MIGRATIONS APLICADAS NO RAILWAY: não verificado aqui (comando railway run prisma migrate status ficou pendente / prisma 7 download)
SIDEBAR EM PRODUÇÃO: depende do deploy do serviço flowos-web após push do commit que contém PortalSidebar (já em main anterior)

SEED STAGES: 12→15 corrigido? SIM — seed usa PIPELINE_STAGES (15) de @flow-os/templates + CAIXA_PIPELINE_SLA_DAYS

PARALELAS:
  Cenário encontrado: C (não havia modelo dedicado)
  Estrutura atual: Deal.meta (JSON) para flags; paralelas também persistidas em deal_parallels
  Implementação:
    - model DealParallel + enums ParallelType / ParallelStatus + migration 20260413190000_add_deal_parallels
    - packages/templates/deal_parallel_rules.ts — desiredParallelActivation (P-01)
    - seed: upsert dealParallel por deal demo após criar deal

FUNIL FILTRÁVEL:
  Campo de stage no schema: Deal.stageId (FK → Stage)
  GET /api/leads aceita stageIds[]: SIM (+ alias stageIds)
  FunnelBar implementado: SIM — GET ?funnel=1 + chips na LeadsTable (clique / Shift+clique)

BOOTSTRAP: 15 stages + paralelas: SIM para stages (caixa); paralelas não são por workspace — só seed/sync por deal

TYPECHECK: OK em apps/web (pnpm exec tsc --noEmit). packages/db tsc tem limitações de rootDir com imports workspace (pré-existente); seed corre em tsx.

OLHAR DE ÁGUIA:
  - Templates já tinham PIPELINE_STAGES com 15 itens; o gap era seed (12) desalinhado — corrigido importando templates no seed.
  - prisma generate local pode falhar com EPERM no Windows (query_engine DLL bloqueada).
  - Railway: aplicar migrate deploy para 20260413120000, 20260413140000, 20260413190000 (e anteriores pendentes) antes de usar deal_parallels em produção.
```
