# Sprint 9 — Devolutiva (2026-04-14)

```
SCHEMA: MissionProfile criado? SIM
SEED: 4 perfis padrão criados? SIM (bootstrap workspace novo: Missão Simplificada, Padrão, Missão Complexa, Área de Risco)
TELA /motoboys/perfis: abre? SIM (lista + links novo/editar)
CRIAR PERFIL: funciona? SIM (POST /api/mission-profiles via /motoboys/perfis/novo)
EDITAR ITENS: drag-reorder? PARCIAL (↑/↓ em vez de drag nativo; campos base/bônus/skip por item ainda não na UI)
PALAVRAS-CHAVE: seleção automática? SIM (resolver + seed "Área de Risco" com keywords em autoSelectRules)
DISPATCHER: usa perfil correto? SIM (resolveMissionProfileForDeal + profileId no assignment)
PWA: recebe profile configurado? VERIFICAR (GET /api/vistoria/[token] implementado; UI PWA aceite/badge — confirmar no app móvel)
TYPECHECK: OK / erros — ERROS legados no @flow-os/web (AuditTab, WorkflowEditor, field-workflows, etc.); corrigido profileId null vs undefined em field-agent-dispatcher/followup
DEPLOY: SUCCESS? (commit hash) — não executado nesta sessão
OLHAR DE ÁGUIA: lacunas identificadas?
  - Workspaces já existentes sem bootstrap de perfis precisam migração/script one-off.
  - Tela de edição ainda não cobre skipReasons, autoSelectRules (UF/cidade/prioridade), valores centavos por item, skip livre, clone via UI além do prompt na lista.
  - Parte 4 UI (deal/campanha: badge sugestão IA, tabela leads) pendente além da API GET .../mission-profile-suggestion.
  - Prisma generate no Windows pode falhar com EPERM se DLL em uso.
```

## Ferramentas usadas nesta continuação

Cursor: **Read**, **Write**, **StrReplace**, **Grep**, **ReadLints**, **Shell** (sem MCP).
